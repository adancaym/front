import { ModelPermisionApi } from './model.permision.api';

describe('ModelPermisionApi', () => {
  it('should create an instance', () => {
    expect(new ModelPermisionApi()).toBeTruthy();
  });
});
