import { ControllerPermisionApi } from './controller.permision.api';

describe('ControllerPermisionApi', () => {
  it('should create an instance', () => {
    expect(new ControllerPermisionApi()).toBeTruthy();
  });
});
