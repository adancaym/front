import { CurrencyModelCoreApi } from './currency.model.core.api';

describe('Currency', () => {
  it('should create an instance', () => {
    expect(new CurrencyModelCoreApi()).toBeTruthy();
  });
});
