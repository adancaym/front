import { EndpointCoreInternal } from './endpoint.core.internal';

describe('Endpoint', () => {
  it('should create an instance', () => {
    expect(new EndpointCoreInternal('')).toBeTruthy();
  });
});
