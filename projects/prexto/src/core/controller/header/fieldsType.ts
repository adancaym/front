export enum FieldsType {
  Date = 'date',
  PrimaryKey = 'primary',
  ForeignKey = 'foreignkey',
  Email = 'email',
  Text = 'text',
  Array = 'array',
  ArrayForeignKey = 'arrayForeignkey',
  Object = 'object',
  Empty = '',
  Password = 'password',
}
