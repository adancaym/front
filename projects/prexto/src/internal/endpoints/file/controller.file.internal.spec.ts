import { ControllerFileInternal } from './controller.file.internal';

describe('Controller', () => {
  it('should create an instance', () => {
    expect(new ControllerFileInternal()).toBeTruthy();
  });
});
